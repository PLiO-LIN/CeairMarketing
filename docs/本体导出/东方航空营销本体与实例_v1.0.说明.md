# 东方航空营销本体与实例导出说明 v1.0

导出时间：2026-09-22T11:20:37+08:00

主文件：东方航空营销本体与实例_v1.0.owl

## 导出范围

本次导出当前本地数据库 `services/platform-api/ceair-marketing.db` 中所有租户的本体实体与关系，以及当前代码语义注册表。未访问服务器，未混入测试数据库，也没有新造实例。
本次只读取数据，没有回写数据库。账号、密码、模型 API Key、会话信息等表不在导出范围中。
本地现有实例的关系证据包含 fixture-flight-products，属于已有的航班产品样例；不是全部线上经营数据。

## 数量

| 项目 | 数量 |
| --- | --- |
| 租户 | 1 |
| 已注册业务类 | 41 |
| 已注册关系类型 | 44 |
| 已注册的类属性（按类分别定义） | 209 |
| 实例中的扩展属性（按类去重） | 56 |
| OWL 数据属性总数 | 265 |
| 业务实例 | 11 |
| 数据库关系记录 | 13 |
| 独立关系断言 | 13 |
| 动作 / 函数 / 智能域契约声明 | 14 / 13 / 6 |
| RDF 三元组 | 3384 |
| 导出校验提示 | 0 |

## 在本体编辑器中如何查看

用支持 OWL RDF/XML 的编辑器打开主文件即可，例如 Protege。不需要启动平台或访问 IRI 对应的网站。
1. Classes：查看全部业务类及中文名称；某些类当前没有实例，仍保留完整定义。
2. Object properties：查看关系名称及 Domain/Range；多个允许类型使用 unionOf 并集。
3. Data properties：查看每类对象的属性；同名字段按对象类型分别定义，避免错误混用领域。
4. Individuals：查看实际实例及其数据属性、对象关系。原数据库中的英文实例名称保持原样。
5. Annotations：查看来源、租户、置信度、任务标识、原始 JSON 和更新时间；关系证据保存在 OWL 断言注释中。
6. Ontology annotations：查看 actionDefinition、functionDefinition、agentContract 和完整 registryJson。

## 映射和边界

- 对象类型导出为 owl:Class，关系导出为 owl:ObjectProperty，属性导出为 owl:DatatypeProperty，实际实例导出为 owl:NamedIndividual。
- 实例 IRI 包含租户 ID 和 external_id，不会合并不同租户的同名对象。OWL 文件本身没有平台访问控制，请按内部资料管理。
- 不额外创造类继承、互斥、唯一性、必填、逆关系、传递性或基数约束；当前注册表没有定义这些 OWL 公理。
- Domain/Range 用于 OWL 推理，不等同于闭世界数据校验。此导出不意味着已经完成 DL 一致性推理或 SHACL 校验。
- 属性定义未声明强类型，所以 Range 为 rdfs:Literal；实际数值、布尔值按 JSON 原类型写入。日期字符串不猜测时区。
- 嵌套对象、数组保持 JSON 字符串，不擅自拆成新实体；null 不生成虚假数值，完整原始值保存在 rawAttributesJson。
- 同一关系有多条来源记录时，关系断言按 RDF 去重，各记录的来源、证据和置信度分别保留为 owl:Axiom 注释。
- 只有属性里出现对象 ID，不等于数据库已有明确关系；不推测补边，也不把所有业务表自动转换成本体。
- 实例中出现的未注册属性会作为 observed-extension 保留，不能误认为已完成标准化建模。
- 动作、函数、智能域契约作为注释保留，而不是伪装成可执行规则或额外业务实例。
- RDF/XML 序列化完成后会重新解析并验证图同构，检查序列化没有丢失三元组。

## 当前实例清单

| 租户 | 类型 | 实例名称 | external_id |
| --- | --- | --- | --- |
| 1 | 产品标签 | Early bird | product-label-L1 |
| 1 | 产品分组 | Fare bundle | product-group-G1 |
| 1 | 活动产品 | Early fare | product-P1 |
| 1 | 辅营产品 | Extra baggage | ancillary-XB1 |
| 1 | 机场 | Shanghai | airport-SHA |
| 1 | 机场 | Shenzhen | airport-SZX |
| 1 | 航线 | Shanghai-Shenzhen | route-4f35d0c31e71062feb |
| 1 | 航班 | MU5101 | flight-deae2d4d75857c1081 |
| 1 | 航段 | MU5101 | segment-58f8e3f72e37381397 |
| 1 | 舱位 | Economy | cabin-b68bf567c4ef83e425 |
| 1 | 运价 | Y 680 | fare-00601e9d0ab9562801 |

## 没有实例的业务类

市场信号（MarketSignal）、市场（Market）、经营指标观测（MetricObservation）、营销机会（Opportunity）、营销目标（MarketingObjective）、客户需求（CustomerNeed）、客户聚合（CustomerAggregate）、可配置业务属性（ConfigurableAttribute）、客群快照（AudienceSnapshot）、产品包（ProductPackage）、价值主张（ValueProposition）、营销策略方案（StrategyPlan）、触点计划（TouchpointPlan）、营销内容（ContentAsset）、营销经营事项（MarketingCase）、营销活动（Campaign）、活动版本（CampaignVersion）、审批任务（ApprovalTask）、执行批次（ExecutionBatch）、触达渠道（Channel）、营销反馈（Feedback）、营销归因结果（AttributionResult）、效果复盘（Review）、营销建议（Recommendation）、业务规则（BusinessRule）、业务证据（Evidence）、人工决策（HumanDecision）、智能体运行（AgentRun）、知识文档（KnowledgeDocument）、知识片段（KnowledgeChunk）、知识事实（KnowledgeClaim）

这些类已经定义，但当前本地图谱表没有对应实例，因此没有伪造个体来填满全流程。

## 导出检查提示

未发现关系端点缺失、跨租户引用、未注册类型或关系端点类型不符。

## 业务表范围对照

| 业务表 | 本地记录数 |
| --- | --- |
| campaigns | 15 |
| campaign_versions | 15 |
| marketing_opportunities | 0 |
| audience_packages | 0 |
| audience_snapshots | 0 |
| audience_tags | 113 |
| product_packages | 20 |
| content_assets | 0 |
| approval_tasks | 5 |
| execution_batches | 5 |
| knowledge_documents | 0 |
| knowledge_chunks | 0 |
| market_hotspots | 16 |
| data_pipeline_jobs | 2 |

上述计数仅帮助判断本地图谱是否覆盖业务，业务表行不会被重复或推测性投影成本体实例。

## 重新生成

在项目根目录执行（需要安装 rdflib）：

```powershell
python scripts/export_marketing_ontology.py
```

脚本默认拒绝覆盖现有文件。新一版可使用 --version v1.1；确需重新生成同一快照可使用 --overwrite。

## 版本记录

| 版本 | 内容 |
| --- | --- |
| v1.0 | 从本地语义注册表和本体数据库导出完整 OWL 评审快照 |
